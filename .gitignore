# 個人機敏資料（請勿上傳）
API.env
*.pfx

# Python
__pycache__/
*.pyc
*.pyo

# macOS
.DS_Store

# PCF 本地快取（每次成功抓取都會覆寫，屬於「每台機器自己的」暫存）
# 不可以進版控：不然某台機器抓到的舊淨值會被同步到另一台，
# 那台就算爬蟲壞了也看不出來，只會安靜地顯示過期的淨值。
# 已用 `git rm --cached 00981A.json 00403A.json` 從索引移除。
00981A.json
00403A.json

# PCF 本地備份的新家（每台機器自己的執行期快取）
.pcf_cache/
